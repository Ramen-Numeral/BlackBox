package game.languageProcessing;
//map the possible commands to embeddings at start up,
// /send user commands to the open ai embeddings api, then compare for a match
//use streams for embeddings
//the goal is to handle homynyms etc match incomplete user input
public class Embeddings {
}
